let mensagem: string = 'Hello World, Kimberli Sabino';
console.log(mensagem);

// Para transpilar o código existem duas formas:
// 1 - chegar na pasta onde está o arquivo ts e rodar tsc .\nomedoarquivo.ts (ele vai gerar um arquivo js na mesma pasta)
// 2 - criar o arquivo tsconfig.json (recomendado) > ctrl+shift+b > tsc: build > no terminal: node .\nomedoarquivo.js